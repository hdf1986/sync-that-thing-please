// mod os_tools;
mod packages;

fn main() {
  // let os_info = os_tools::OsInfo::fetch_info();
  // let os_version = os_info.version().unwrap();
  // let os_name = os_info.name().unwrap();

  // println!("{} {}", os_name, os_version);
  // println!("{}", os_version)
  // println!("{}", packages::is_available("code"))
}
